name: VPS Free via Cloudflare RDP

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Update and install packages
      run: |
        sudo apt update && sudo apt install -y wget curl unzip openssh-server xrdp xfce4 xfce4-goodies
        sudo systemctl enable xrdp --now

    - name: Create user tuan
      run: |
        sudo useradd -m tuan
        echo "tuan:tuan123" | sudo chpasswd
        sudo usermod -aG sudo tuan
        echo "AllowUsers tuan" | sudo tee -a /etc/ssh/sshd_config
        sudo service ssh restart
        echo "RDP sẵn sàng với username: tuan và password: tuan123"

    - name: Install Cloudflared
      run: |
        wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
        sudo dpkg -i cloudflared-linux-amd64.deb

    - name: Start Cloudflare Tunnel for RDP
      run: |
        echo "🚀 Đang khởi chạy Cloudflare Tunnel (RDP)..."
        nohup cloudflared tunnel --url tcp://localhost:3389 --protocol auto --no-autoupdate > tunnel.log 2>&1 &
        sleep 5
        echo "===== Nội dung tunnel.log ====="
        cat tunnel.log | grep -Eo "tcp://[a-zA-Z0-9.-]+:[0-9]+"

    - name: Keep VPS Alive
      run: |
        echo "VPS đang chạy trong 6 tiếng..."
        sleep 21600
